# Optional: point to a hosted mock JSON array of user records (500 items recommended).
# Each record should match `UserDetailRecord` in `src/types/user.ts`.
# If unset, the app uses a deterministic in-browser catalog of 500 users.
# VITE_USERS_API_URL=https://run.mocky.io/v3/your-mock-id
